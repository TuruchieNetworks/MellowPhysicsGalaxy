// import React from 'react';
// import { usePhysics } from './path/to/hooks';

// const PhysicsComponent = ({ spheres, deltaTime, radius, cubeSize, gravity, dampingFactor }) => {
//   const substeps = 10; // Increase for more precision

//   usePhysics(spheres, deltaTime, substeps, radius, cubeSize, gravity, dampingFactor);

//   return null; // Or return your canvas or other components
// };

// export default PhysicsComponent;
